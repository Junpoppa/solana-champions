# Copy to .env.local for local dev, or set in your host's env for prod builds.
# WebSocket URL of the multiplayer lobby server (server/).
# Dev default when unset: ws://<current-hostname>:8787
# Prod example: wss://your-server.example
VITE_WS_URL=ws://localhost:8787
